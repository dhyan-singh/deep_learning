[project]
name = "dl-utils"
version = "0.1.0"
description = "Utility functions for my ML project"
authors = [{name = "Dhyan"}]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["dl_utils*"]